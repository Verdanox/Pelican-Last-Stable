This Repo is used for the last Beta of Pelican when it didnt had that much Bugs.

CMD:

```bash
curl -L https://github.com/verdanox/pelican-last-stable/releases/latest/download/panel.tar.gz | sudo tar -xzv
